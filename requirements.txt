yt-dlp
httpx==0.28.1
google-genai==1.75.0
python-dotenv==1.2.2
tqdm==4.67.3

# Vertical reframing (face/subject tracking)
opencv-python
numpy
mediapipe
scenedetect[opencv]
# Optional: enable --no-yolo to disable; install ultralytics only if you want
# person-detection fallback when no face is visible (pulls in PyTorch).
# ultralytics

# Backend API server
fastapi
uvicorn[standard]
python-multipart

# Viral hook overlay rendering
Pillow

# YouTube Shorts upload (free, YouTube Data API v3)
google-api-python-client
google-auth-oauthlib
google-auth-httplib2

# User accounts + auth (register/login, per-user API keys)
sqlmodel>=0.0.22
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-multipart==0.0.20
